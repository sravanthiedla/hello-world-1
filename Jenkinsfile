
pipeline {
  agent any
  stages('CI') {
    stage('Checkout') {
      steps {
        echo 'Checkout'
    }
  }

   stage('Build') {
     step {
       echo 'Checkout'
     }
   }
   stage('Test') {
     step {
       echo 'Test'
     }
   }
 }
}
