pipeline {
  agent any
  stages('CI') {
    stage('Checkout') {
      steps {
        echo 'Check'
    }
  }

   stage('Build') {
     steps {
       echo 'Checkout'
     }
   }
   stage('Test') {
     steps {
       echo 'Test'
     }
   }
 }
}
